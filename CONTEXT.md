# Project Context & User Preferences

## User Preferences
- **Server Restarts**: When a change requires a server restart (e.g., changing build scripts, server config), do it automatically. Do not ask the user to do it.
